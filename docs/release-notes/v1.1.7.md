## What's changed

- Reopening a session now flags a tool call the model only wrote out instead of making — previously only live narration was caught.
- Quota cards keep their status reason across the rename of that field; accounts saved by earlier versions still read correctly.
- The test suite is now typechecked as part of `npm run typecheck`.
- Chat transcript rows extracted into `FeedRow`, each kind now covered by a rendering test.
- Four technical debt entries closed.
